# ============================================================
# 仓库只放源码与文档。下面这些要么是可重建的产物，要么体积巨大
# 且与本工具无关，一律不进仓库。
# ============================================================

# ---- 构建产物 ----
build/
**/build/
.gradle/
**/.gradle/

# ---- 第三方 jar（由 tools/build.ps1 取回）----
*.jar
!app/chunker/gradle/wrapper/gradle-wrapper.jar

# ---- 构建输出的发布目录 ----
# 带前导斜杠 = 只认仓库根目录。
# 不能写成 test/，那会连 app/chunker/cli/src/test/ 一起排除，
# 那里是上游的测试代码与测试地图（16 MB），必须保留。
/dist/
/release/

# ---- 工具运行时生成 ----
work/
tools/bin/
workbench/bin/
**/__pycache__/
*.pyc

# ---- 交付压缩包（只认仓库根目录，不影响测试地图）----
/*.zip

# ---- 临时文件 ----
*.patch
tmp/

# ---- IDE ----
.idea/
.vscode/
*.iml
*.iws
*.ipr

# ---- 系统 ----
.DS_Store
Thumbs.db
desktop.ini

# ---- Vantaloom 会话状态 ----
.vantaloom/

# ---- 外部 AI 的交接包 ----
# 另一个 AI 改不了仓库，把改动源码＋补丁＋实验记录打包发过来。
# 它里面是待审查的材料（含 4 MB 实验数据与截图），不是仓库内容，
# 合入的内容要经过审查后写进真实源码路径；包本身原地保留备查。
/*_changes/
MinecraftMapConversionTool_changes/

# ============================================================
# 以下是 Chunker 上游带的东西，本工具用不到。
#
# cli/data       各版本方块/物品数据表。607 MB（bedrock 367 MB + java 240 MB）。
#                本工具做 Java → 1.12.2 降级，运行时不加载它。
#                但要注意：cli/src/test 里的 5 个 IdentifierValidationTests
#                会读它来校验映射表，全新克隆（如 CI）下这两个目录都不存在，
#                那些测试会**跳过**（已改为 assumeTrue），不会失败。
# app/chunker/app 上游的 Electron + React 界面，含 580 MB 数据副本。
#                本工具用的是自建网页界面（cli/src/main/resources/web）。
#
# 实测：移开这两块后 gradlew :cli:shadowJar 构建成功，
# 产物与不移开时字节完全相同（31926310 字节）。
# 注意那只证明「编译」不受影响，测试会受影响（见上）。
# 详见 docs/UPSTREAM.md
# ============================================================
app/chunker/cli/data/
app/chunker/app/
