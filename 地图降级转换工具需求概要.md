# Minecraft 高版本地图转换为 1.12.2 工具

## 1. 项目定位

这是一个面向建筑外观的 Minecraft Java 版地图降级转换工具。

工具的目标不是完整保留高版本地图的游戏机制，而是把高版本地图转换为能够在 Java 版 1.12.2 中打开的地图，并尽可能保留建筑的整体形状、布局和视觉效果。

项目参考：[HiveGamesOSS/Chunker](https://github.com/HiveGamesOSS/Chunker)。

优先考虑复用或扩展 Chunker 已有的格式解析、区块读写、版本支持、映射配置和 CLI 转换能力，而不是从零实现完整的 Minecraft 存档解析系统。

## 2. 目标范围

### 2.1 输入和输出

- 输入：Minecraft Java 版原版地图。
- 首要支持范围：Java 版 1.13 及以上、工具已知的版本。
- 输出：Minecraft Java 版 1.12.2 地图。
- 输出地图应当可以被原版 1.12.2 客户端或服务端正常加载。
- 转换始终写入新的输出目录，不修改原始地图。
- 建筑坐标默认保持不变。

### 2.2 核心目标

- 尽可能保留建筑的整体外观。
- 尽可能保留方块的形状和朝向。
- 支持用户自定义高版本方块到 1.12.2 方块的替换规则。
- 支持转换前扫描地图并列出未知方块。
- 对无法转换的内容进行替换、删除或记录，而不是写入 1.12.2 无法理解的数据。
- 生成详细的转换报告，说明哪些内容被替换、删除或裁剪。

## 3. 明确不需要保留的内容

以下内容不属于第一目标，可以直接删除或清理：

- 生物、怪物、村民、宠物。
- 掉落物、经验球、盔甲架等普通实体。
- 现代实体的 AI、属性、装备和行为数据。
- 箱子、潜影盒、熔炉、漏斗等容器内部的物品。
- 玩家背包和大部分玩家状态。
- 进度、统计、袭击、POI 等游戏状态。
- 数据包和现代战利品表。
- 大部分结构生成数据。
- 不兼容的现代命令方块命令。

注意：箱子、熔炉等方块本身需要保留，内部物品不需要保留。转换时可以保留一个空容器，并清理其中的现代 NBT 数据。

## 4. 需要特殊保留的内容

### 4.1 展示框

展示框是唯一需要保留的普通实体类型。

应尽量保留：

- 展示框位置。
- 展示框朝向。
- 展示物品旋转角度。
- 展示框中的物品。
- 物品在 1.12.2 中不存在时的替换结果或空展示框。

高版本的发光展示框在 1.12.2 中不存在，可以转换为普通展示框。

除展示框外，其他实体默认删除。

展示框中的物品需要单独拥有物品映射规则，因为现代物品可能在 1.12.2 中不存在。

## 5. 方块转换规则

### 5.1 内置映射

工具应当提供一套内置的常见方块映射，用于处理明显没有对应物的高版本方块。

示例：

```text
minecraft:cherry_planks  -> minecraft:oak_planks
minecraft:deepslate      -> minecraft:stone
minecraft:copper_block   -> minecraft:iron_block
```

内置映射只作为默认值，不能覆盖用户配置。

### 5.2 用户自定义映射

用户可以通过 JSON 配置文件覆盖或补充映射规则。

概念示例：

```json
{
  "blocks": {
    "minecraft:cherry_planks": "minecraft:birch_planks",
    "minecraft:deepslate": "minecraft:black_concrete",
    "minecraft:sculk": "minecraft:purple_wool"
  },
  "items": {
    "minecraft:echo_shard": "minecraft:quartz"
  }
}
```

最终配置格式可以根据 Chunker 的 `blockMappings` 机制调整。

### 5.3 映射优先级

方块映射建议按照以下顺序处理：

1. 精确的方块状态映射。
2. 用户配置的方块名称映射。
3. 内置方块映射。
4. 默认近似方块。
5. 用户选择的“替换为空气”或“停止转换”。

用户配置的规则优先级最高。

### 5.4 方块状态

转换不能只转换方块名称，还需要尽量转换方块状态，包括：

- 朝向。
- 楼梯的上下方向。
- 楼梯形状。
- 台阶类型。
- 门和活板门的开关状态。
- 栅栏、墙和玻璃板的连接状态。
- 铁轨方向。
- 红石元件的方向和基础状态。
- 原木的轴向。

1.12.2 不支持的状态应当被清理或替换为最接近的合法状态。

### 5.5 水logged 等无法共存的状态

现代版本允许部分方块同时具有方块和水logged状态，而 1.12.2 无法表示这种组合。

默认策略应当优先保留建筑方块本身，舍弃无法共存的水状态。该行为可以在以后增加为可配置选项。

## 6. 高度范围处理

1.18 及以上版本的世界高度范围与 1.12.2 不同。

默认策略：

- 保留 1.12.2 能够表示的高度范围。
- 保持原始 Y 坐标，不整体平移建筑。
- 超出目标高度范围的方块直接裁剪。
- 在报告中记录被裁剪的方块数量、最低高度和最高高度。

不建议默认整体平移地图，因为这会影响建筑坐标、命令方块位置和地图内部的相对坐标。

## 7. 方块实体处理

方块实体和普通实体分开处理。

### 7.1 需要保留方块本身的容器

以下方块应当保留外观，但可以清空内部数据：

- 箱子。
- 陷阱箱。
- 熔炉。
- 高炉。
- 烟熏炉。
- 漏斗。
- 发射器和投掷器。
- 潜影盒。

容器内部物品不需要转换和保留。

### 7.2 可以逐步支持的视觉数据

以下数据不是第一阶段必须，但未来可以增加：

- 告示牌文字。
- 旗帜图案。
- 书与笔的显示内容。
- 地图物品。
- 蜂箱或其他有视觉状态的方块实体。

如果现代 NBT 会导致 1.12.2 无法读取，应优先清理 NBT，保留方块本身。

## 8. 应当删除或重建的数据

为了保证输出地图能被 1.12.2 正常读取，应当对现代数据进行筛选，而不是直接复制：

- 不直接复制现代区块 NBT。
- 不只修改 `DataVersion` 后输出。
- 删除 1.12.2 不认识的区块字段。
- 删除不兼容的实体数据。
- 清理容器物品列表。
- 清理现代数据包、POI 和进度数据。
- 根据 1.12.2 的格式重新写出区块、调色板、群系和高度相关数据。
- 必要时重新计算或清理高度图和光照缓存。

## 9. 自定义维度和世界生成

第一阶段只保证以下维度：

- 主世界。
- 下界。
- 末地。

现代自定义维度无法直接表示为 1.12.2 的原生维度。第一阶段可以采用以下策略之一：

- 转换前报告并跳过。
- 让用户指定映射到主世界、下界或末地。
- 将其导出为单独的转换结果。

已经生成的建筑可以转换，但不能保证 1.12.2 中新生成的区块与高版本地形一致。

## 10. 转换流程

```text
读取地图
  ↓
识别 Java 版本和数据格式
  ↓
扫描所有区块，统计方块和实体
  ↓
生成未知方块与潜在损失报告
  ↓
加载内置映射和用户映射
  ↓
转换区块和方块状态
  ↓
清理方块实体数据
  ↓
只恢复展示框实体
  ↓
处理高度裁剪和维度映射
  ↓
写出 1.12.2 地图
  ↓
校验输出文件
  ↓
生成最终转换报告
```

## 11. 转换模式

建议提供以下模式：

### strict

发现未映射方块或严重不兼容数据时停止，不生成最终地图。

适合在用户希望先完善映射表时使用。

### safe

尽可能转换，未知方块使用用户指定的默认方块或替换为空气，并记录所有损失。

适合作为默认模式。

### visual

优先保留建筑的视觉效果，即使需要牺牲方块功能、容器内容和游戏机制。

适合建筑展示地图。

## 12. 转换报告

输出目录中应生成报告，例如：

```text
conversion-report.json
conversion-report.txt
```

报告至少包括：

- 源版本。
- 目标版本。
- 处理的 region 数量。
- 处理的区块数量。
- 方块总数量。
- 每种方块的出现次数。
- 每种替换方块的来源和目标。
- 未映射方块及出现次数。
- 被删除的实体数量。
- 被保留的展示框数量。
- 被删除的容器物品数量。
- 被裁剪的方块数量。
- 被跳过的维度。
- 转换错误和对应坐标。

还可以额外生成一个待填写映射文件：

```text
unmapped-blocks.json
```

用户补充映射后，可以重新执行转换。

## 13. 性能和可靠性要求

- 按 region 或区块流式处理，不把整张地图读入内存。
- 对相同的源方块状态缓存转换结果。
- 使用有上限的并发任务，避免占满内存。
- 原始地图只读，输出地图单独写入。
- 支持大地图的进度显示。
- 单个 region 转换失败时记录错误，不应静默跳过。
- 写入后校验 NBT、区块和 region 文件。
- 最终使用 1.12.2 客户端或服务端进行实际加载测试。
- 任何未识别数据都必须进入报告。

## 14. 第一阶段建议目标

第一阶段不追求支持所有游戏数据，而是完成一个稳定的建筑转换闭环：

1. 支持 Java 版常见高版本地图输入。
2. 输出可被 1.12.2 打开的地图。
3. 完成普通方块和方块状态转换。
4. 支持用户自定义方块映射。
5. 保留箱子等容器方块，但清空物品。
6. 删除除展示框之外的实体。
7. 保留普通展示框和发光展示框的建筑展示效果。
8. 处理高度裁剪。
9. 生成完整转换报告。
10. 建立一批典型建筑和特殊方块的自动化测试地图。

## 15. 暂不解决的问题

- 模组地图的通用转换。
- Java 与基岩版之间的转换。
- 现代命令自动降级为 1.12.2 命令。
- 自定义维度的完整还原。
- 红石行为的完全一致。
- 村民交易、AI 和实体行为。
- 未来版本在尚未添加支持时的自动兼容。

## 16. 最终目标描述

本工具最终应当成为一个以建筑外观为中心的高版本地图降级工具：

用户提供一个高版本 Java 地图，工具先告诉用户地图中有哪些 1.12.2 不支持的方块和数据；用户可以通过映射表决定这些方块应当替换成什么；工具随后生成一个可在 Java 1.12.2 中打开的建筑地图。

地图中的实体默认舍弃，只保留展示框；箱子、熔炉等容器保留方块外观，但不保留内部物品。
