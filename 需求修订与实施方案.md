# 地图降级转换工具 — 需求修订与实施方案

> 本文档修订《地图降级转换工具需求概要.md》。
> 原文档写于「复用 Chunker + 命令行工具」的假设之下；经源码核实、端到端实测与 5 轮决策，前提已发生变化，故逐项修订。
> 文中所有技术结论均标注实测依据，未实测的部分明确标为「待验证」。

---

## 1. 已确认的关键决策

| # | 决策项 | 结论 |
|---|---|---|
| 1 | 输入版本范围 | **Java 1.13 起全支持** |
| 2 | 交付形态 | **Web 应用**，界面参考 BlueMap，要求「可阅览所有操作」 |
| 3 | 未映射方块 | **接受静默替换为空气**（不做 strict 中止） |
| 4 | 高度处理 | **默认整体向上平移**，把源地图最低点对齐到 Y=0；超出 1.12.2 的 256 上限部分**自动裁剪** |
| 5 | 预览形式 | **左右并排对比**：左＝修改前（源地图），右＝修改后（转换结果） |

### 与原需求的冲突点（已按上表覆盖）

| 原文档 | 原表述 | 修订为 |
|---|---|---|
| §6 高度范围 | 「不建议默认整体平移地图」 | **默认就是要平移**（改为平移 + 裁剪 + 报告） |
| §11 转换模式 | 提供 strict / safe / visual 三种 | strict 降级为可选开关，**safe 为默认** |
| 全文 | 假定为 CLI 工具 | **Web 应用** |

---

## 2. 整体架构

系统由三层构成，转换核心不自研，预览层复用 BlueMap：

```
┌───────────────────────────────────────────────────────────┐
│  Web 界面（浏览器）                                        │
│  ┌──────────────────────┬──────────────────────────────┐  │
│  │  左：修改前           │  右：修改后                   │  │
│  │  BlueMap v5.16        │  BlueMap v1.5.5              │  │
│  │  渲染源地图            │  渲染转换后的 1.12.2 地图      │  │
│  └──────────────────────┴──────────────────────────────┘  │
│  操作区：选择地图 / 转换进度 / 映射表编辑 / 转换报告          │
└───────────────────────────────────────────────────────────┘
            │  HTTP
            ▼
┌───────────────────────────────────────────────────────────┐
│  服务层                                                    │
│  · 任务调度（转换任务排队、进度上报）                        │
│  · 双 BlueMap 编排（各自独立配置与 webroot，分别起停）        │
│  · 报告聚合（JSON / 可视化）                                │
└───────────────────────────────────────────────────────────┘
            │  Java 调用
            ▼
┌───────────────────────────────────────────────────────────┐
│  转换核心：Chunker（fork 后改造）                           │
│  · 已具备：区域读写、版本识别、方块/状态映射、数字 ID 写出    │
│  · 需补：Y 轴平移、转换报告、未映射方块统计                   │
└───────────────────────────────────────────────────────────┘
```

### 2.1 为什么预览要挂两个 BlueMap

BlueMap 自身按 Minecraft 版本分开发布产物，**没有任何单一版本能同时渲染源地图和 1.12.2 结果**。实测确定的组合：

| 用途 | 版本 | 所需 Java | 覆盖的 MC 版本 |
|---|---|---|---|
| 左：源地图 | **v5.16** | 21（本机已有） | 1.13.2 – 1.21.11 |
| 右：转换结果 | **v1.5.5** | 8（本机已有） | **1.12.2** – 1.17 |

- v1.5.5 是**最后一个支持 1.12.2 的版本**。依据：v1.6.3 发布说明原文「Dropped Minecraft 1.12.2 support.」，其支持表为 `1.6.3-cli.jar | 1.13.2 - 1.17.1`；而 v1.5.5 的支持表为 `1.5.5-cli.jar | 1.12.2 - 1.17`。
- 选 v5.16 而非最新的 v5.24，是因为 v5.17 起要求 Java 25（本机没有），而 v5.16 只需 Java 21，且已覆盖到 1.21.11。
- **两版数据目录布局不同**（实测）：旧版输出 `web/data/<mapId>/`，新版输出 `web/maps/<mapId>/`。因此两者必须各自使用独立的 webroot，不能共用目录。

### 2.2 已知的体验风险（需决策）

v1.5.5（2021-06）与 v5.16（2026-02）**相隔约 5 年**，两个内置 Web 前端（webapp）的视觉风格与控制方式必然不同。直接并排会出现「左右像两个产品」的问题。可选处理方式：

| 方案 | 说明 | 代价 |
|---|---|---|
| A. 接受差异 | 直接并排，各自原样 | 无，但观感割裂 |
| B. 外框调和 | 用 iframe 嵌入，外层统一配色/标题/尺寸 | 小，无法统一内部控件 |
| C. 给旧版换肤 | 改 v1.5.5 的前端资源以贴近新版 | 中，需改老前端构建 |
| D. 新版兼容 legacy | 让 v5.16 支持 1.12.2 | 大，需改核心读图逻辑 |

---

## 3. 实测依据

以下均为本机实际运行结果，不是推断。

### 3.1 Chunker 的降级通道本身是好的

实测 `1.15.2 → 1.12.2`：

- 转换成功，输出 **149 万**非空气方块、**426 种** `id:data` 组合
- 方块状态位正确保留：楼梯朝向（`id=53:data=2/3`）、草方块变种（`id=2:data=7`）、混凝土颜色（`id=251:data=8`、`id=252:data=7`）
- 结论：**方块映射、状态转换、数字 ID 写出这套核心可以直接复用，不必自研。**

### 3.2 但 1.18+ 地图会转出空地图（最高优先级问题）

实测 `1.20.5 → 1.12.2`：

- **输出非空气方块数 = 0**
- 转换器**没有报任何错**，退出码 0，只在末尾提示转换完成
- 原因定位：源地图方块全部位于 **Y = -64 ~ -49**（1.18+ 世界下限为 -64），而 Chunker 的写入判断是 `isChunkHeightSupported(y) = y ∈ [0, 16)`，超出范围的区块**被直接丢弃且不记录**
- 输出中仅残留 122 个实体（画 104、展示框 18）与 971 个方块实体（旗帜 352、床 158、头颅 140、箱子 83…）

**据此确认决策 4 的必要性**：不实现 Y 轴平移，1.18+ 地图的转换结果等于一张空地图。

### 3.3 BlueMap v1.5.5 能正确渲染转换结果

实测用 v1.5.5 渲染 Chunker 输出的 1.12.2 地图：

- 渲染成功，退出码 0，耗时约 33 秒（9 个 region、约 9216 区块）
- 自动下载 Mojang 客户端资源（实测为 `minecraft-client-1.12.0.jar`）用于取贴图
- 产出真实区块数据（`hires` 目录下的分块文件）
- 前提：`core.conf` 中 `accept-download: true`

**结论：左右并排预览方案技术可行，已打通。**

---

## 4. 必须自研的能力（Chunker 的缺口）

| 能力 | Chunker 现状 | 我们的工作 |
|---|---|---|
| **Y 轴平移** | **完全没有**（转换相关代码中检索 `offset`/`shift` 零命中） | 需自行实现，含方块、实体、方块实体坐标同步平移 |
| **转换报告** | **没有**（无任何报告类） | 从零构建，需在转换链路各环节打点统计 |
| **未映射方块统计** | 方块未映射时**静默回落为空气**，不报错不记录 | 需挂钩子统计并输出清单 |
| 未映射物品/实体统计 | **已有**（会打印 `Missing item mapping` 等） | 只需捕获这些输出并结构化 |
| 映射配置 | **已有且很强**：支持标识符映射、状态分组、类型值表三层 | 直接复用，Web 界面包装即可 |
| 版本识别 | **已有**（`DETECT_VERSION` 消息） | 直接复用 |
| 进度上报 | **已有**（`ProgressResponse`/`ProgressStateResponse`） | 直接复用 |

### 4.1 Y 轴平移的实现要点

1. **平移量对齐到 16 的倍数**：区块按 16 格一个 section 存储，对齐后平移等价于改 section 索引；否则需逐块搬移，易错且慢。最终效果一致（最低点 ≥ 0）。
2. **实体与方块实体坐标必须同步平移**：箱子、熔炉、展示框、旗帜在文件中存的是**绝对坐标**，只平移方块会导致它们漂移到错误位置。
3. **裁剪统计**：平移后超出 256 的部分需裁剪，并记录数量与 Y 范围。
4. **代价说明**：1.18+ 世界高度为 384 格（-64~319），平移到 0 后**超出 256 的 128 格必然被裁掉**。地表建筑（Y≈63 → 平移后 127）不受影响，但建于 Y=200 以上的高层建筑会被削顶。

---

## 5. 分阶段实施

### 阶段 0：骨架与端到端闭环

- 搭建工程结构（fork Chunker 作为转换核心）
- 集成双 BlueMap，各自独立配置与 webroot
- 搭建左右并排页面与基本操作区
- **验收**：指定一张源地图 → 触发转换 → 左右并排看到两个渲染结果（此阶段 1.18+ 仍会因未修 Y 偏移而空白，属预期）

### 阶段 1：Y 轴平移（最高优先级）

- 扫描源地图计算最低方块 Y
- 计算对齐 16 的平移量
- 在转换链路中应用平移（方块 + 实体 + 方块实体）
- 实现超限裁剪与统计
- **验收**：`1.20.5 → 1.12.2` 能转出有内容的、可在 1.12.2 中正常显示的地图

### 阶段 2：转换报告

- 统计：方块映射明细、未映射方块、删除的实体、清空的容器、裁剪数量与 Y 范围、平移量
- 输出 JSON，并在网页中可视化
- **验收**：用户能清楚看到「哪些变了、变了多少、为什么」

### 阶段 3：映射配置

- 复用 Chunker 的映射机制，提供内置默认映射
- Web 界面编辑映射规则
- 「未映射方块清单」→ 补写映射 → 重新转换 的闭环
- **验收**：用户能自主控制转换结果

### 阶段 4：打磨

- 大图性能、进度细化、错误处理
- 1.13–1.17 全版本回归验证
- 真机 1.12.2 客户端加载测试

---

## 6. 仍未确定的事项

1. **Web 部署形态**：单机本地运行（浏览器开 localhost），还是部署到服务器供多人使用？后者需增加上传、任务队列、多用户隔离，工作量差异较大。
2. **地图输入方式**：网页上传压缩包，还是服务端填写本地路径？（若为单机，填路径最省事；服务端上传大地图需分片）
3. **两个 BlueMap 的观感调和**：见 §2.2 的 A/B/C/D 四选一。
4. **平移规则细节确认**：是否接受「最低点对齐到 0」导致的建筑整体位移？是否有场景需要改为「按建筑范围平移」以减少裁切损失？
5. **真机验证条件**：是否有可运行的 1.12.2 客户端或服务端用于最终加载测试？

---

## 7. 与原需求的其余差异说明

原文档中以下内容经核实**无需修改**，仍然成立：

- §3 明确不保留的内容（实体、容器物品、进度统计等）——与 Chunker 的裁剪能力方向一致，仅需补报告
- §4.1 展示框需保留——Chunker 已支持普通展示框；**发光展示框在 1.12.2 分支缺处理器，需补**
- §5.4 方块状态需转换——实测证实 Chunker 已正确处理朝向、变种、颜色等状态位
- §5.3 映射优先级——可直接映射到 Chunker 的 `identifiers` / `state_lists` / `types` 三层机制
- §13 性能要求——Chunker 本身按 region 流式处理、带缓存与并发上限
